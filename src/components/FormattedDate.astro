---
interface Props {
	date: Date;
  name: string;
}

const { date, name } = Astro.props;
---

<time datetime={date.toISOString()} transition:name={name}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
